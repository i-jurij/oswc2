security:
    authentication:
        cookieName: userId 
        #cookieDomain: 'example.com'   # (string|domain)
        #cookieSamesite: None          # (Strict|Lax|None) defaults to Lax

constants:
    TABLE: 'user'